#include <stdio.h>
#define MAXCLEN 255

int main(void)
{
    int c, i, iascii;
    int nchar[MAXCLEN];

    iascii = 0;
    for (i = 0; i < MAXCLEN; ++i)
        nchar[i] = 0;

    while ((c = getchar()) != EOF){
        nchar[c]++;
    }

    for (i = 0; i < MAXCLEN; ++i){
        printf("%c | ", i);
        for 
    }
    printf();

    return 0;
}
