#include <stdio.h>

/* Count the number of lines */
int main(void)
{
    int nl = 0;
    char c;
    while ((c = getchar()) != EOF && c = '\n')
        nl++;
    printf("The number of li%d.\n", nl);
    return 0;
}
