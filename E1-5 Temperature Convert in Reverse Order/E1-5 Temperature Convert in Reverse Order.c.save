#include <stdio.h>

/* Exercise 1-5 Modify the temperature conversion program to print
    the table in reverse order, that is, from 300 degrees to 0. */
int main(void)
{
    int fah_temp;
    for (fah_temp = 300; fah_temp <= 0; fah_temp--){
        printf("%3d\t%");
    }

    return 0;
}
